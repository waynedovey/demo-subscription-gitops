apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-placement
  labels:
    app.kubernetes.io/name: nginx-a-uw1
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: nginx-a-uw1
  template:
    metadata:
      labels:
        app.kubernetes.io/name: nginx-a-uw1
    spec:
      containers:
      - name: nginx
        image: quay.io/jpacker/nginx:1.16.1
        ports:
        - containerPort: 8080
        imagePullPolicy: Always
        env:
        - name: CLOUD_PROVIDER
          value: "Amazon"
        - name: ZONE
          value: "us-west-1"
        volumeMounts:
          - mountPath: /app/provider
            name: cache-provider
      volumes:
      - name: cache-provider
        emptyDir: {}
